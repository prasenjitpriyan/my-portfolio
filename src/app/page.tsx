import React from "react";
import Hero from "@/components/Hero";

const Home: React.FC = () => {
  return (
    <main className="min-h-screen">
      <Hero />
    </main>
  );
};

export default Home;
